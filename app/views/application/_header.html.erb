
<nav class="navbar navbar-inverse navbar-static-top header">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <% if params[:for].present? %>
        <a class="navbar-brand" href="/">Treview for <%= params[:for] %></a>
      <% else %>
        <a class="navbar-brand" href="/">Treview</a>
      <% end %>

      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target=".navbar-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <ul class='nav navbar-nav navbar-right collapse navbar-collapse'>
      <% if current_user %>
        <li class='text-right'>
          <%= link_to "My Videos", "/users/#{current_user.id_with_name}" %>
        </li>
        <li class='text-right'><a href='/logout'>Sign out</a></li>
      <% else %>
        <li class='text-right'><a href='/auth/facebook'>Sign in with Facebook</a></li>
        <!-- <li class='text-right'><a href='/auth/twitter'>Twitter</a></li> -->
      <% end %>
    </ul>
  </div>
</nav>
